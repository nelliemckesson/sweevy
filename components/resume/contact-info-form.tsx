'use client';

import { useState } from 'react';
import { setContactInfo } from "@/lib/db";

export async function ContactInfoForm(props) {
  return (
    <div className="flex-1 w-full flex flex-col gap-12 border border-foreground/50">
      
    </div>
  );
}
